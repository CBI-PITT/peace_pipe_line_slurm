# Agent Notes

- 2026-06-12: Updated backend `AGENTS.md` to require reading root and backend notes before changes and appending backend notes after backend file changes.
- 2026-06-15: Added backend reader plugin `reader_plugins/ome_zarr_reader/` with metadata extraction, TIFF slice export, and 100um volume generation for `.ome.zarr` datasets stored as top-level `scale0`, `scale1`, etc. arrays.
- 2026-08-27: Added optional backend job/workflow history updates behind `PEACE_ENABLE_JOB_HISTORY`. The watcher now records dispatching failures, persists returned SLURM job IDs/provenance/log folders for single jobs and readers, and stores per-step workflow dispatch metadata needed for Home-page tracking and forked reruns from a chosen step.
- 2026-09-02: Added validated preprocessing z-range utilities and absolute-index SLURM submission across stretch, histogram equalization, blur, gamma, resize, image calculator, Cellpose denoise, FFT stripe removal, and rembg; subset outputs retain source z-indices and publish `processed_z_range` provenance.
- 2026-09-02: Reused unsuffixed Cellpose raw min/max caches for full-availability inputs and reserved range suffixes for genuinely sparse upstream inputs.
- 2026-09-15: Added atlas-region aggregation to `transform_points`. The `get_structures_df.py` worker now also writes `{cells_basename}_region_counts.csv` (columns `atlas_structure_name`, `atlas_structure_acronym`, `atlas_structure_number`, `cell_count`) with one row per unique valid atlas region sorted by count descending; outside-atlas points are excluded. `main.py` added the matching `out_counts_csv_path` and includes it in the resume check. Per-point CSV and binary TIFF outputs unchanged; no provenance or Flask changes.
- 2026-09-16: Restored the `if not input_path: return None` guard at the top of `ensure_input_metadata` in `start_pipeline.py`. The guard (documented in the 2026-09-10 note) had been lost in a later edit, which broke CSV-input operations like `transform_points` that submit empty `input`/`output`: the watcher raised `FileNotFoundError` on a relative `.dataset_info.json` path before the operation could fall back to the cells CSV provenance. Behavior for non-empty inputs unchanged.
- 2026-09-16: Fixed `transform_points` worker `KeyError: 'full_resolution'`. Operation provenance writers propagate `resolution`/`shape` but not `full_resolution` (only readers do), so `get_structures_df.py` crashed computing the `*_raw_px` columns for reader->operation->detection chains. Added `resolve_full_resolution` which uses the metadata's own `full_resolution` when present, otherwise walks the provenance `source` chain (cycle-safe) and finally falls back to `resolution`. Verified against the failed task's chain (denoise -> imaris_reader, resolves to `[2.0, 0.6, 0.6]`).
- 2026-09-16: Added `nearest_neighbor` operation (`operations/nearest_neighbor/`: `main.py` + `do_nn.py`, registered in `operations/__init__.py`). CSV input via `cells_path` kwarg; falls back to the CSV-adjacent `.dataset_info.json` for base dirs; `channel`/`resolution_level`/`sequence` from that provenance. Worker auto-detects coordinate style (`z_raw/y_raw/x_raw` µm directly; `axis-0/1/2` scaled by provenance `resolution` with cycle-safe chain walk, `ValueError` if unresolvable), computes 3D NN distance in µm via `scipy.cKDTree` (k=2, self excluded, duplicates 0, non-finite rows NaN), writes `{basename}_nn_distances.csv` (original columns + `nearest_neighbor_distance_um`); when atlas columns are present also writes `{basename}_nn_region_stats.csv` (name/acronym/number, `cell_count`, `mean_nn_distance_um`, `median_nn_distance_um`; outside-atlas excluded; count-desc sort). Single CPU SLURM job, dbscan env, HIGH_RAM, cores=8/mem=64; resume check covers both expected outputs. Verified locally: synthetic spotiflow/dashboard CSVs, duplicate/NaN rows, chain resolution, missing-resolution error, main.py header detection + resume path.
- 2026-09-16: Reimplemented `mean_intensity` (`operations/mean_intensity/`: `main.py` + `do_mean_intensity.py` orchestrator + `run_mean_intensity_z_layer.py` worker, registered in `operations/__init__.py`; backend sources had been lost, only stale `__pycache__` remained). CSV input via `cells_path` + `radius` (um) kwargs; raw source resolved from the CSV provenance chain by walking `source` while it is an existing `.dataset_info.json` file (walk ends at the raw reader node since reader provenances point `source` at non-JSON paths); raw TIFF folder from reader `output.path` with fallbacks; validates raw level/resolution/shape x/y vs chain with clear `ValueError`s. `main.py` submits one CPU orchestrator (peace env, cores=8/32Gb) which selects valid z layers (`round`ed z voxels, `half_z = ceil(radius/res_z)`, full-sphere-in-z only, edges ignored), writes the z-layer array script, submits via `submit_slurm_indices` (cores=4/32Gb, task id = z center, resume skips existing partials), polls, and concats partials into the final CSV. Workers read only the needed planes via `resolve_tiff_path`, use a µm-based sphere kernel with clipped y/x gather (partial spheres at y/x borders yield values), and write `partial_dfs/part_mean_intensity_z_{zfill(5)}.csv`. Final CSV (only processed points) under `<base_output_dir>/mean_intensity/resolution_level_X/channel_Y/<csv_sequence>,mean_intensity_r{radius}/`, column `mean_intensity_r{radius}`. Verified locally: synthetic TIFF series + chains, sphere means vs brute-force reference (incl. border clipping), edge exclusion, merge, validation failures, resume paths, CLI arg alignment.
- 2026-09-21: Added the `remove_background` operation (`operations/remove_background/`: `main.py` + self-contained `do_remove_background.py`, registered in `operations/__init__.py`), converting the user's root-level `brain_bg_2d_smooth_edges.py` SAM2 script into a per-z-layer preprocessing operation modeled on the `rembg` plugin. kwargs: `user`, `priority` (default '2'), `prerequisites`, `z_start`/`z_end` via `normalize_z_range`. Outputs under `<output>/remove_background/resolution_level_X/channel_Y/`: `removed_background{prev_op}{z_suffix}` (feathered background-removed images, original dtype; provenance `output` points here) and `bg_mask{prev_op}{z_suffix}` (uint8 0/255 masks), both with canonical `r{rl}_t00_c{ch}_z{z}.tif` names. `main.py` writes `remove_background_rl{rl}_c{channel}.sh` (activates `/h20/home/lab/miniconda3/bin/activate remove_bg_py311_cuda124`) and submits one GPU SLURM array job per z-layer via `submit_slurm_indices(range(z_start, z_end), partition=gpu, needs_gpu=True, cores=4, memory=32)` with resume via the intersection of existing z-indices in BOTH output folders and `--depend afterok` for prerequisites. Worker resolves the input plane via `resolve_tiff_path`, loads `facebook/sam2.1-hiera-small` once per job, and runs the full SAM2 pipeline (robust normalize -> downsample -> Otsu box prompt -> SAM2 -> clean -> bilinear upscale with edge_smoothing_px=2 -> feathered multiply) copied from `brain_bg_2d_smooth_edges.py` (file left untouched). Verified locally: py_compile all files, class import/registration, stubbed-SAM2 end-to-end worker run on synthetic TIFFs (mask 0/255, dtype preserved, background removed, canonical names), orchestrator tests (folders, provenance sequence/processed_z_range/parameters, submit kwargs, subset z-suffix, resume intersection, prerequisites, multi-step sequence suffix). Note: `transformers`/SAM2 weights must be installed in the `remove_bg_py311_cuda124` env on GPU nodes before real runs; watcher restart needed after deploy.
- 2026-09-23: Security hardening of the watcher after a P0 audit. New import-light `utils/validation.py` (no numpy/pandas so the tests run without the analysis env) with: `valid_user` (lab-username regex, kills newline/shell-metacharacter injection into SLURM job scripts via extras.user), `path_allowed_for_user` (realpath containment under {FS_ROOT}/{user}/ incl. symlinks — replaces the self-referential `startswith` check whose USER came from the same crafted JSON as the paths, and kills `..` traversal + prefix collisions like /h20/Public/iana-evil), `resolve_operation_class` (whitelist: only ImageOperation/ImageReader subclasses or the registered plugin dicts dispatch — no more bare `getattr(sys.modules[__name__], OPERATION)`), and `history_record_path` (derives the history path from job_record_id/workflow_id + submitted_by instead of trusting `history_file` from the task JSON, which allowed rewriting arbitrary .json files; record ids must match a hex/uuid pattern). start_pipeline.py: all three handlers (start_pipeline_slurm, start_reader_slurm — which previously had NO path/user check at all — and start_workflow_slurm per-step) plus update_submission_history_for_failure now use these helpers; workflow steps validate their extras user + input/output paths. New pytest suite `tests/` (pytest.ini, conftest + test_validation.py, 13 tests) covering all the helpers (traversal, prefix collision, symlink escape, injection strings, crafted history payloads). Verification note: task JSONs written by hand to the watched folder must now carry a valid `user` (regex) and paths under /h20/Public/<user>/; SLURM job scripts still interpolate user strings without quoting (shell-injection surface intentionally left open per user decision) and payload signing was skipped — both remain open items.
- 2026-09-23: Closed the shell-injection surface in the generated SLURM job scripts (the last deferred P0 item). New `shell_arg(value)` in `utils/slurm.py` (shlex.quote + newline stripping — safe for both bash command lines and #SBATCH directive lines; intentional runtime variables like $SLURM_ARRAY_TASK_ID must NOT go through it). Swept ALL 30 script generators (24 operations + plugins/rembg + 5 readers) via `utils/quoting_sweep.py` (a one-shot line-based, quote-aware transformer kept in utils/ for reference; run it only after a git checkout of operations/plugins/reader_plugins — it is idempotent-safe against already-transformed lines): every user-derived interpolation now goes through shell_arg — `#SBATCH -J` job names ({self.user}-suffix and {self.user}-{self.name} forms), `#SBATCH -o/-e` log paths (%j stays literal for sbatch), the `python do_*.py` command with all path/extras args (input, output, save_folder, points, masks, cells_path, results_folder, metadata_path, registration folders, output_folder_sequence, volume_100um_location, atlas, orientation, brain_geometry, model, user, priority, experiment, metadata key/value pairs, resolution floats, settings.HOME). $SLURM_ARRAY_TASK_ID/$SLURM_JOB_ID left raw (runtime expansion). Int-valued writes (z_start/z_end, int()-forced ball-z-size) left as-is. New tests `tests/test_slurm_quoting.py` (9 tests): shlex.split round-trip for hostile payloads, newline stripping, a REAL bash behavioral test (command substitution inside a quoted arg must not execute — mutation-verified: the naive double-quote pattern DOES execute), a simulated script-generation check (hostile newline in extras.user injects no bash line), benign-value transparency. Backend suite now 22 passed. Watcher restart needed after deploy.
